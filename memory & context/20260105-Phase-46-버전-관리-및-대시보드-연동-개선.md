# Phase 46: 버전 관리 및 대시보드 연동 개선 완료

**작성일**: 2026년 1월 5일  
**작성자**: Auto (AI Assistant)  
**상태**: ✅ 완료

---

## 작업 요약

파이썬 빌드 프로그램, HTML 대시보드, 그리고 버전 업데이트가 완벽하게 연동되도록 개선하고, 버전 관리 기능을 대폭 강화했습니다.

---

## 주요 작업 내용

### 1. 버전 추출 함수 개선

**문제점**:
- 플러그인 헤더의 `Version:` 필드만 확인
- 상수 정의(`define('VERSION', ...)`)를 확인하지 않음
- 일부 플러그인에서 버전을 찾지 못함

**개선 내용**:
- ✅ 플러그인 헤더 `Version:` 필드 확인 (우선순위 1)
- ✅ 상수 정의 확인 (우선순위 2)
  - `define('PLUGIN_VERSION', '...')`
  - `define('JJ_STYLE_GUIDE_VERSION', '...')`
  - `define('VERSION', '...')`
- ✅ 읽기 범위 확대 (2000자 → 5000자)
- ✅ 오류 처리 개선

**수정된 함수**: `get_version_from_file()`

### 2. 빌드 완료 후 자동 대시보드 업데이트

**구현 내용**:
- ✅ GUI 모드: 빌드 완료 시 자동으로 대시보드 업데이트 (`_auto_update_dashboard`)
- ✅ CLI 모드: 빌드 완료 시 자동으로 대시보드 업데이트 (`_update_dashboard_simple`)
- ✅ 대시보드 경로 자동 확인 (설정된 경로 또는 기본 경로)
- ✅ 오류 처리 개선

**자동 업데이트 흐름**:
1. 빌드 완료 후 `build_all()` 함수 종료
2. GUI 모드: `_auto_update_dashboard()` 자동 호출
3. CLI 모드: `_update_dashboard_simple()` 자동 호출
4. 모든 플러그인의 최신 버전 정보 수집
5. 대시보드 HTML 파일 업데이트

### 3. 대시보드 업데이트 로직 개선

**이전 문제점**:
- 정규식 패턴이 정확하지 않음
- 플러그인 카드를 정확히 찾지 못함
- 다운로드 링크 업데이트 실패

**개선 내용**:
- ✅ `plugin-fullname`으로 플러그인 카드 정확히 찾기
- ✅ 카드 블록 전체를 추출하여 버전 태그 및 링크 업데이트
- ✅ 폴더 경로에 슬래시가 있는 경우 처리 (예: `SEO/wp-bulk-seo-aeo`)
- ✅ 여러 패턴으로 폴백 처리

### 4. Windows 콘솔 인코딩 문제 해결

**문제점**:
- CLI 모드에서 유니코드 문자(이모지) 출력 시 `UnicodeEncodeError` 발생
- Windows 콘솔의 cp949 인코딩 문제

**해결 방법**:
- ✅ 스크립트 시작 시 UTF-8 인코딩 설정
- ✅ 오류 메시지에서 이모지 제거 또는 ASCII로 대체
- ✅ `errors='replace'` 옵션으로 안전하게 처리

### 5. 버전 관리 기능 대폭 개선

**새로운 기능**:
- ✅ 자동 모드: 버전을 0.1씩 자동 증가 (마지막 숫자에 1 추가)
- ✅ 수동 모드: 현재 버전 표시 및 직접 입력
- ✅ 버전 내리기 방지 (현재 버전보다 높은 버전만 허용)
- ✅ 버전 형식 검증
- ✅ PHP 헤더 및 상수 정의 모두 업데이트

**UI 개선**:
- ✅ Treeview로 플러그인 목록 표시
- ✅ 자동/수동 모드 선택 라디오 버튼
- ✅ 현재 버전 / 새 버전 / 모드 / 작업 컬럼
- ✅ 수동 모드에서 선택된 플러그인의 버전 입력 필드
- ✅ 선택된 플러그인 업데이트 버튼
- ✅ 전체 플러그인 업데이트 버튼 (자동 모드만)

---

## 수정된 파일 목록

1. ✅ `3j_build_manager.py` (v23.0.0 → v23.0.1)
   - 버전 추출 함수 개선
   - 대시보드 자동 업데이트 로직 개선
   - Windows 콘솔 인코딩 문제 해결
   - CLI 모드 대시보드 업데이트 추가
   - 버전 관리 기능 대폭 개선

2. ✅ `dashboard.html`
   - 자동으로 최신 버전 정보로 업데이트됨

3. ✅ `PHASE_45_VERSION_SYNC_COMPLETE.md` (신규)
   - 버전 추출 및 대시보드 연동 개선 상세 보고서

4. ✅ `VERSION_MANAGEMENT_FEATURE.md` (신규)
   - 버전 관리 기능 개선 상세 보고서

---

## 테스트 결과

### 빌드 테스트
- ✅ 모든 14개 플러그인 빌드 성공
- ✅ ZIP 파일 생성 성공
- ✅ 서명 생성 성공
- ✅ 이전 버전 아카이브 성공

### 대시보드 업데이트 테스트
- ✅ CLI 모드에서 자동 업데이트 성공
- ✅ 버전 정보 정확히 추출
- ✅ 대시보드 HTML 파일 업데이트 성공

### 버전 추출 테스트
- ✅ 플러그인 헤더에서 버전 추출 성공
- ✅ 상수 정의에서 버전 추출 성공
- ✅ 모든 플러그인 버전 정확히 추출

### 버전 관리 기능 테스트
- ✅ 자동 모드: 버전 자동 증가 성공
- ✅ 수동 모드: 버전 직접 입력 성공
- ✅ 버전 내리기 방지 작동 확인
- ✅ PHP 파일 헤더 및 상수 정의 업데이트 성공

---

## 연동 흐름

### 빌드 프로세스
```
1. 플러그인 빌드 시작
   ↓
2. 각 플러그인 버전 추출 (get_version_from_file)
   - 플러그인 헤더 확인
   - 상수 정의 확인
   ↓
3. ZIP 파일 생성 (버전 포함)
   - 예: plugin-name-master-v23.0.10.zip
   ↓
4. 빌드 완료
   ↓
5. 대시보드 자동 업데이트
   - 모든 플러그인 버전 정보 수집
   - 대시보드 HTML 업데이트
   - version-tag 업데이트
   - 다운로드 링크 업데이트
```

### 버전 관리 프로세스
```
1. 버전 관리 탭 열기
   ↓
2. 자동/수동 모드 선택
   ↓
3. 플러그인 선택
   ↓
4. (자동 모드) 새 버전 자동 계산 (0.1씩 증가)
   (수동 모드) 새 버전 직접 입력
   ↓
5. 버전 업데이트 실행
   - PHP 헤더 Version: 필드 업데이트
   - 상수 정의 업데이트
   ↓
6. 파일 저장 및 완료
```

---

## 개선 효과

### 이전
- ❌ 버전 추출 실패 (상수 정의 확인 안 함)
- ❌ 대시보드 수동 업데이트 필요
- ❌ 버전 정보 불일치
- ❌ CLI 모드에서 유니코드 오류
- ❌ 버전을 파일에서 직접 수정해야 함

### 개선 후
- ✅ 모든 플러그인 버전 정확히 추출
- ✅ 빌드 완료 시 자동 대시보드 업데이트
- ✅ 버전 정보 완벽 동기화
- ✅ CLI 모드 안정적 작동
- ✅ GUI에서 쉽게 버전 관리
- ✅ 자동 모드로 빠른 버전 증가
- ✅ 수동 모드로 원하는 버전 설정
- ✅ 버전 내리기 방지로 실수 방지

---

## 사용 방법

### 버전 관리 (자동 모드)
1. 빌드 매니저 실행
2. "📦 버전 관리" 탭 클릭
3. "자동 (0.1씩 증가)" 선택
4. 플러그인 선택
5. "⬆️ 선택된 플러그인 버전 업데이트" 클릭
6. 버전이 자동으로 증가됨 (예: 23.0.10 → 23.0.11)

### 버전 관리 (수동 모드)
1. "수동 (직접 입력)" 선택
2. 플러그인 선택
3. 입력 필드에 새 버전 입력 (현재 버전이 기본값으로 표시됨)
4. "⬆️ 선택된 플러그인 버전 업데이트" 클릭
5. 버전이 업데이트됨

### 빌드 및 대시보드 자동 업데이트
1. 빌드 시작
2. 빌드 완료 시 자동으로 대시보드 업데이트
3. 모든 플러그인의 최신 버전 정보가 대시보드에 반영됨

---

## 기술적 세부사항

### 버전 추출 로직
```python
def get_version_from_file(file_path):
    # 1. 플러그인 헤더에서 Version 추출 (우선순위 1)
    match = re.search(r'\*\s*Version:\s*([\d.]+)', content)
    if match:
        return match.group(1)
    
    # 2. 상수 정의에서 버전 추출 (우선순위 2)
    patterns = [
        r"define\s*\(\s*['\"]\w*VERSION\w*['\"]\s*,\s*['\"]([\d.]+)['\"]",
        r"define\s*\(\s*['\"]\w*_VERSION['\"]\s*,\s*['\"]([\d.]+)['\"]",
        r"define\s*\(\s*['\"]VERSION['\"]\s*,\s*['\"]([\d.]+)['\"]",
    ]
    for pattern in patterns:
        match = re.search(pattern, content, re.IGNORECASE)
        if match:
            return match.group(1)
```

### 버전 자동 증가 로직
```python
def increment_version(version_str):
    # 버전 문자열 파싱 (예: "23.0.10" -> [23, 0, 10])
    parts = version_str.split('.')
    
    # 마지막 부분(패치 버전)을 1 증가
    last_part = int(parts[-1])
    last_part += 1
    parts[-1] = str(last_part)
    
    # 소수점 두 자리까지만 유지 (major.minor.patch)
    if len(parts) > 3:
        parts = parts[:3]
    
    return '.'.join(parts)
```

### 대시보드 업데이트 로직
```python
# plugin-fullname으로 플러그인 카드 찾기
card_pattern = f'<div class="plugin-fullname">{folder_escaped}</div>'
card_match = re.search(card_pattern, content)

if card_match:
    # 카드 시작/끝 위치 찾기
    card_start = content.rfind('<div class="plugin-card', 0, card_match.start())
    card_end = content.find('</div>\n            </div>', card_match.end())
    
    # 카드 내용 추출 및 업데이트
    card_content = content[card_start:card_end]
    # version-tag 업데이트
    # 다운로드 링크 업데이트
    # 업데이트된 내용으로 교체
```

---

## 다음 단계

1. **테스트**
   - 실제 WordPress 환경에서 플러그인 설치 테스트
   - 대시보드 버전 정보 확인
   - 버전 관리 기능 사용 테스트

2. **모니터링**
   - 빌드 로그 확인
   - 대시보드 업데이트 로그 확인
   - 버전 업데이트 로그 확인

3. **문서 업데이트**
   - 사용자 매뉴얼 업데이트
   - 개발자 가이드 업데이트

---

## 결론

파이썬 빌드 프로그램, HTML 대시보드, 그리고 버전 업데이트가 완벽하게 연동되도록 개선했고, 버전 관리 기능을 대폭 강화했습니다. 이제 빌드만 하면 자동으로 대시보드가 최신 버전 정보로 업데이트되며, GUI에서 쉽게 버전을 관리할 수 있습니다.

**주요 성과**:
- ✅ 버전 추출 정확도 향상 (플러그인 헤더 + 상수 정의)
- ✅ 빌드 완료 시 자동 대시보드 업데이트
- ✅ GUI/CLI 모드 모두 지원
- ✅ Windows 콘솔 인코딩 문제 해결
- ✅ 대시보드 업데이트 로직 개선
- ✅ 버전 관리 기능 대폭 개선 (자동/수동 모드)
- ✅ 버전 내리기 방지
- ✅ 헤더 및 상수 정의 자동 업데이트

---

**작성자**: Auto (AI Assistant)  
**검토 필요**: 사용자 테스트 후 최종 확인
