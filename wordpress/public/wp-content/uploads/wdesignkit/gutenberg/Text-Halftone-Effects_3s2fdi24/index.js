class MyClass_rr8aob25 {

        constructor() {
            window.addEventListener('DOMContentLoaded', (event) => {
                var $this = this;
        setTimeout(() => {
            let main_html = jQuery('.wkit-wb-Widget_3s2fdi24');

            jQuery.each(main_html, function (idx, scope) {
                $this.main_function_rr8aob25(jQuery(scope));
            });
        }, 800);
            });
        }

        main_function_rr8aob25($scope) {
        let is_editable = wp?.blocks ? true : false;
            
 
        }
    }

    new MyClass_rr8aob25();